<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.CyanBridgeManager" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- CyanBridge AI branding (MVP) -->
        <item name="colorPrimary">@color/cyan_accent</item>
        <item name="colorPrimaryVariant">@color/card_bg</item>
        <item name="colorSecondary">@color/cyan_accent</item>

        <item name="android:windowBackground">@color/bg_dark</item>
        <item name="android:statusBarColor">@color/bg_dark</item>
        <item name="android:navigationBarColor">@color/card_bg</item>

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- ECHO runs entirely in Compose and paints its own near-black ground, so
         this theme only needs to keep the platform out of the way: no action
         bar, and a window background that matches EchoColors.bg (#080808) so
         there is no light flash between launch and first frame. -->
    <style name="Theme.Echo" parent="Theme.MaterialComponents.NoActionBar">
        <item name="android:windowBackground">@color/echo_bg</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>
